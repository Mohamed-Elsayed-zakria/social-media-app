import 'package:flutter/material.dart';

class UploadeVideoReelsScreen extends StatelessWidget {
  const UploadeVideoReelsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}